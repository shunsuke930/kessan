# 要件定義書：税理士事務所向け決算対応ダッシュボード

## 1. 背景・目的

現在、顧客管理はGoogleスプレッドシートで行っているが、法人顧客は決算期がバラバラであるため、
「いつから対応・連絡を始めるべきか」が一目で把握できない。結果として対応が遅れ、
決算申告が遅延するリスクが存在する。

本アプリは、決算期に基づく対応スケジュールをWBS/ガントチャート形式で可視化し、
遅延リスクのある案件を一目で把握できるダッシュボードを提供することを目的とする。

## 2. 対象ユーザー

| 項目 | 内容 |
|---|---|
| 利用規模 | 社内・チーム向け（10名程度） |
| 権限 | 全員が全案件を閲覧可能（担当者ごとの制限なし） |
| 認証 | Googleアカウント（Google Workspace想定） |

## 3. 機能要件

### 3.1 ダッシュボード表示
- 顧客（法人）ごとの決算期に紐づくタスクをWBS/ガントチャート形式で表示
- 遅延しそうな案件・タスクは色分け表示（例：通常＝グレー、要注意＝黄色、遅延＝赤）
- 通知機能（メール・チャット等）は不要。ダッシュボード上の視覚表示のみで対応

### 3.2 タスク管理
- 決算日を起点に、テンプレートから標準的な対応タスク・期限を自動生成
- 生成されたタスクは案件ごとに個別編集・調整が可能（テンプレート＋個別調整のハイブリッド方式）

### 3.3 データ連携
- 既存スプレッドシートの列構成：顧客名・法人名、決算月・決算日、担当税理士・担当者
  （現状1シートに全顧客が1行ずつ並ぶ構造。対応ステータス列はまだ存在しない）
- アプリはスプレッドシートに対して**読み取り中心**で連携する
  - 顧客情報（名前・決算日・担当者）はスプレッドシートをマスタとして取得
  - タスク・進捗情報はアプリ側（GAS内のデータストア、または専用シート）で管理し、
    既存の顧客管理シートは編集しない方針とする（双方向同期による競合を避けるため）

### 3.4 タスクテンプレート仕様
- 対応開始：決算日の**90日前**から着手
- 標準タスク例（決算日を起点に順序立てて発生）：
  1. 問い合わせ先への事前連絡（必要書類依頼など）
  2. 貸借対照表・資料作成
  3. 申告書作成
  4. 問い合わせ先への確認・承認取得
  5. 申告・納税
- 各タスク間の細かい日数配分は今後詳細を詰める（開始・終了の大枠は確定）

### 3.5 遅延判定・色分けルール
- 判定方式：期限までの日数による自動判定 ＋ 手動での上書き修正の両方に対応
- 判定基準日：決算日、および申告・納税期限（決算日の2ヵ月後）
- 色分けルール：
  | 状態 | 条件 |
  |---|---|
  | 通常（グレー） | 決算日の90日前より先 |
  | 要注意（黄色） | 決算日の30日前を切った時点 |
  | 遅延（赤） | 申告・納税期限（決算日の2ヵ月後）を過ぎた時点 |
- メール・チャット等の通知機能は不要

## 4. 非機能要件

- **実装方式**：Google Apps Script（GAS）ベースのWebアプリ
  - 理由：既存スプレッドシートとのネイティブ連携が可能、追加のサーバー・インフラが不要、
    小規模チームでの運用コストを抑えられる
- **ホスティング**：GASのWebアプリ機能を利用（追加サーバー不要）
- **アクセス制御**：Googleアカウントベースの認証（Workspaceドメイン内に限定する等は今後検討）

## 5. 技術方針（提案）

| 項目 | 内容 |
|---|---|
| バックエンド | Google Apps Script（`SpreadsheetApp`でシート読み書き） |
| フロントエンド | GAS `HtmlService`、またはClaspを使ったローカル開発 |
| ガントチャート描画 | Frappe Ganttなど軽量JSライブラリの利用を想定 |
| 開発ツール | Claude Code + clasp（ローカルでコードを書き、GASにpush） |

## 6. 未確定事項（今後詰める点）

- 各タスク（事前連絡・資料作成・申告書作成・確認取得・申告納税）間の細かい日数配分
- Google Workspaceのアクセス制御の詳細（ドメイン制限の要否など）

## 7. 次のステップ

1. 既存スプレッドシートの実データ（サンプル）を確認し、列名・シート名を実装に反映
2. Claude Codeでの実装開始（GASプロジェクトのセットアップ）
